module Masks
  # Bumped when the surface a consumer depends on changes in a way an older
  # client cannot discover for itself. Published in the discovery document so
  # a consumer checks rather than finds out at the first screen.
  PROTOCOL_VERSION = 1
end
