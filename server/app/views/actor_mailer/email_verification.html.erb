<p class="lede">
  <%= t(".confirm_html", nickname: tag.span(@actor.identifier, class: "strong"), tenant: @tenant_name) %>
</p>

<%= render "action", url: @url, label: t(".action") %>

<p class="meta">
  <%= t(".expiry", duration: ActiveSupport::Duration.build(EmailVerification.lifetime.to_i).inspect) %>
</p>
