<%= @invited_by ? "#{@invited_by.nickname} invited you to #{@tenant_name}." : "You have been invited to #{@tenant_name}." %>

Your username is <%= @actor.nickname %>. Open the link below to choose a
password and finish setting up the account.

<%= @url %>

The link works once, and expires in <%= ActiveSupport::Duration.build(Invitation.lifetime.to_i).inspect %>.
If you were not expecting this, you can ignore it — nothing happens until
somebody opens the link.
