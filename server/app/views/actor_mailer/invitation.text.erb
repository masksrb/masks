<%= @invited_by ? t(".invited_by", nickname: @invited_by.nickname, tenant: @tenant_name) : t(".invited", tenant: @tenant_name) %>

<%= t(".nickname", nickname: @actor.nickname) %>

<%= @url %>

<%= t(".expiry", duration: ActiveSupport::Duration.build(Invitation.lifetime.to_i).inspect) %>
