<%= @invited_by ? "#{@invited_by.nickname} invited you to #{@tenant_name}." : "You have been invited to #{@tenant_name}." %>

Your username is <%= @actor.nickname %>. Open the link to choose a password.

<%= @url %>

Works once, expires in <%= ActiveSupport::Duration.build(Invitation.lifetime.to_i).inspect %>.
Nothing happens until somebody opens it.
