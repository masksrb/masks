<%= @opened_by ? t(".opened_by", nickname: @opened_by.identifier, tenant: @tenant_name) : t(".opened", tenant: @tenant_name) %>

<%= @url %>

<%= t(".expiry", duration: ActiveSupport::Duration.build(PasswordReset.lifetime.to_i).inspect) %>
