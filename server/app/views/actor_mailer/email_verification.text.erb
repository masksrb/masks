Confirm this address for <%= @actor.nickname %> at <%= @tenant_name %>.

<%= @url %>

Works once, expires in <%= ActiveSupport::Duration.build(EmailVerification.lifetime.to_i).inspect %>.
If this was not you, ignore it — nothing else changes.
