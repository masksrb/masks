Confirm that this address belongs to the <%= @tenant_name %> account
<%= @actor.nickname %>.

Until you do, applications you sign in to are told the address has not been
confirmed, and some will refuse it.

<%= @url %>

The link works once, and expires in <%= ActiveSupport::Duration.build(EmailVerification.lifetime.to_i).inspect %>.
If you do not recognise this account, ignore it — confirming is the only thing
the link does, and nothing else changes.
