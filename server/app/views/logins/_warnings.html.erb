<% login.warnings.each do |warning| %>
  <% message = t("logins.warnings.#{warning}", default: nil) %>
  <% if message %>
    <div class="alert alert-error text-sm" role="alert"><%= message %></div>
  <% end %>
<% end %>
