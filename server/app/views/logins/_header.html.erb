<div class="flex flex-col gap-1">
  <h1 class="text-2xl font-bold"><%= heading %></h1>

  <% if login.client&.name %>
    <p class="text-sm opacity-75">
      to continue to <strong class="font-semibold"><%= login.client.name %></strong>
    </p>
  <% end %>
</div>
