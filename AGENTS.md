# Agents

## Writing

All prose in this repo follows [docs/STYLE.md](docs/STYLE.md), which is based on
the Ruby on Rails guides and organized by [Diátaxis](https://diataxis.fr/). That
includes the docs, the READMEs, the locale files in `server/config/locales/`,
and the descriptions the reference pages are generated from: the manage API's
GraphQL descriptions and the documentation in `client/` and `web/`.

- Write plain, declarative sentences in the present tense, in American English
  with the Oxford comma.
- Leave out contrast framing ("X, not Y", "rather than"), closing aphorisms,
  fragments for rhythm, em dashes as general punctuation, metaphor, filler,
  and rhetorical questions.
- Write each page in one Diátaxis mode: the quickstart and demo are
  tutorials, `guides/` holds how-to guides, `concepts/` holds key concepts,
  and `reference/` is reference.
- `manage`, `browser`, `ruby`, and `rails` under `reference/` are generated by
  `./dev reference`. Change the code they read, then regenerate. Never edit
  those pages by hand.
- Use the words in `reference/words.mdx` for the things people see.
- Check every claim against the code before writing it down.
- The repo is public. Leave out real hostnames, addresses, and anything about
  private infrastructure.
