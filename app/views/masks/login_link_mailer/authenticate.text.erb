👋 Enter the following <%= @link.chars %>-character code to log in as "<%= @actor.identifier %>" without using a password:

> <%= @link.code %>

You can also log in automatically with the following URL, providing you open the URL on the same device you're authorizing:

> <%= @link.accept_url %>

Your login code expires in <%= time_ago_in_words(@link.expires_at) %> (<%= @link.expires_at.in_time_zone(@actor.tz).strftime("%b %d at %l:%M%P %Z") %>).
